matrix A = (1,2,3,4;2;2)
matrix B = (1,2,3,4;2;2)

A{
    B = A.Determinant;
    for(int i=0; ((i > 5) && (2 > x)) || x; i++)
    {
        C = A + B;
    }
    if(i == i)
    {
        matrix B = (1,1;1;2)
        B{
            D = B + A;
        }
    }
    A = A :x B;
}

await A {
    this = disfunc(A, B);
}

func matrix disfunc(matrix one, matrix two){
    newmat = newmat :x one;
    return newmat;
}