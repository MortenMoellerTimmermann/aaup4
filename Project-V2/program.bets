matrix A = (1,2,3,4;2;2)
matrix B = (1,2,3,4,1,2,3,4;2;4)

A{
    matrix G = this * this
    int a = 1;

    for (a; a < 10; a++) {
        a += 1;
    }

    while (a < 10) {
        a += 1;
    }

    int b = myf();
}

func int myf () {
    int x = 0;
    return 1;
}